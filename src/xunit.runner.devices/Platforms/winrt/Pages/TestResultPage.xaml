<Page
    x:Class="Xunit.Runners.Pages.TestResultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Xunit.Runners.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utilities="using:Xunit.Runners.Utilities"
    mc:Ignorable="d">

    <Page.Resources>
        <utilities:RunStatusToColorConverter x:Key="RunStatusToColorConverter" />
        <utilities:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        HorizontalScrollBarVisibility="Auto"
        VerticalScrollBarVisibility="Auto"
        HorizontalScrollMode="Auto"
        VerticalScrollMode="Auto"
        Padding="4">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="TestOutputState">
                <VisualState x:Name="NoOutput"/>
                <!-- EDIT TODO <VisualState x:Name="HasOutput">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind HasOutput}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="outputGroup.(UIElement.Visibility)" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>-->
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <StackPanel>
            <Grid>
                <TextBlock Text="{Binding TestCase.DisplayName}" Style="{StaticResource SubheaderTextBlockStyle}" Margin="100,0,0,0"/>
                <Button VerticalAlignment="Stretch" Width="100" Content="Back" HorizontalAlignment="Left" Command="{Binding BackCommand}"/>
            </Grid>

            <Border Margin="0,0,0,10" x:Name="outputGroup" Visibility="{Binding HasOutput, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                <StackPanel>
                    <TextBlock Text="Test Output" />
                    <TextBlock Text="{Binding Output}" />
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding ErrorMessage}" 
                    Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock Text="{Binding ErrorStackTrace}"  
                    Style="{StaticResource BodyTextBlockStyle}"
                    />
        </StackPanel>
    </ScrollViewer>
</Page>
