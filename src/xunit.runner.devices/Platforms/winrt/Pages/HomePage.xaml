<Page
    x:Class="Xunit.Runners.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Xunit.Runners.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:runners="using:Xunit.Runners"
    xmlns:utilities="using:Xunit.Runners.Utilities"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <utilities:RunStatusToColorConverter x:Key="RunStatusToColorConverter" />
        <utilities:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid Margin="4">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="xUnit Test Runner" Style="{StaticResource HeaderTextBlockStyle}" />
        <ProgressBar Grid.Row="1" Height="10" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
        <TextBlock Text="Test Assemblies" Grid.Row="2" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <!-- ItemContainerStyle="{StaticResource ListStyle}" -->
        <ListView Grid.Row="3" SelectionMode="Single"
                  ItemsSource="{Binding TestAssemblies}" SelectionChanged="ListView_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- EDIT TODO x:DataType="runners:TestAssemblyViewModel">-->
                    <StackPanel>
                        <TextBlock Text="{Binding DisplayName}" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock Text="{Binding DetailText, Mode=OneWay}" Foreground="{Binding RunStatus, Converter={StaticResource RunStatusToColorConverter}, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBox IsReadOnly="true" 
                 PlaceholderText="Diagnostic Messages"
                 Grid.Row="3" 
                 TextWrapping="Wrap"
                 Text="{Binding DiagnosticMessages, Mode=OneWay}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 Margin="0,174,0,4"
                 />
        <Grid Grid.Row="5" >
            <Button Content="Run Everything"
                    Command="{Binding RunEverythingCommand}"
                    />
            <Button Content="Credits"                     
                    Command="{Binding CreditsCommand}"
                    HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Page>
